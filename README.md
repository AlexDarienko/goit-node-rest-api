# goit-node-rest-api

Simple REST API for contacts.

Install:
```
npm install
npm start
```

Endpoints:
- GET /api/contacts
- GET /api/contacts/:id
- POST /api/contacts  (body: {name, email, phone})
- PUT /api/contacts/:id (body: any of name/email/phone)
- DELETE /api/contacts/:id
